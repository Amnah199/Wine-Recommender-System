import React, { useState } from "react";
import { Typography } from "@mui/material";
import { CustomModal } from "../CustomModal";

export const TasteCustomization = () => (
  <Typography variant="h5" textAlign={"center"}>
    Taste Customization!
  </Typography>
);
